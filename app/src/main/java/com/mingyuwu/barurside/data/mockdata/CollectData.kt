package com.mingyuwu.barurside.data.mockdata

import com.mingyuwu.barurside.data.Collect

class CollectData {
    object collect {
        val collect = listOf(
            Collect(
                "1", "1", "1", true
            ),
            Collect(
                "2", "1", "1", true
            ),
            Collect(
                "3", "1", "1", false
            ), Collect(
                "4", "1", "1", false
            )
        )
    }

}